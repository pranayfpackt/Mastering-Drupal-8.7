name: Minimal Theme
description: A theme with absolute minimal features.
type: theme
core: 8.x
# All themes are required to have a content region.
regions:
  content: 'Content'
# This theme will not use a parent theme and all templates will
# be pulled from the module's which define the theme hooks
base theme: false
